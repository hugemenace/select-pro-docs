# Flood Fill Select

Flood-fill select connected elements starting from the active element, stopping at any pre-existing selection. Works in vertex, edge, and face mode — the active vertex, edge, or face is used as the seed, and already-selected elements act as barriers that the fill cannot cross.
